message: message from branch dev

spring:
  config:
    import: optional:configserver:http://localhost:8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
    hostname: localhost
server:
  port: 0
management:
  endpoints:
    web:
      exposure:
        include: health, info, refresh
